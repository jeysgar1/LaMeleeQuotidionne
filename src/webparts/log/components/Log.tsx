import * as React from 'react';
import { LogProps } from '../models/LogProps';

export default class Log extends React.Component<LogProps, {}> {
  public render(): React.ReactElement<LogProps> {

    return (
      <div></div>
    );
  }
}
